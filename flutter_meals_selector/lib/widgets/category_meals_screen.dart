import 'package:flutter/material.dart';

class CategoryMealsScreenWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('The reciped is...TBD'),
      ),
      body: Center(
        child: Text('List of recipes in this category'),
      ),
    );
  }
}
